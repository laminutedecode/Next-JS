// Une requête POST est l'une des méthodes utilisées dans HTTP (Hypertext Transfer Protocol) pour envoyer des données à un serveur afin de créer ou de mettre à jour des ressources. Contrairement à une requête GET, qui est principalement utilisée pour récupérer des données, une requête POST est utilisée pour envoyer des données à un serveur afin qu'il les traite. créer la fonction dans route puis  aller dans lextension thunder http://localhost:3000/reviews. (changer get a post > puis aller dans body > json)

export default function Home() {
  return (
     <>
     <h1>Home</h1></>
  );
}
