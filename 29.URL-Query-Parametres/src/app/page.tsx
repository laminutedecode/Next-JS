// parameters sont des paramètres ajoutés à l'URL d'une page. Ils sont utilisés pour passer des informations entre différentes pages ou pour modifier le contenu affiché sur une même page en fonction de ces paramètres. Les query parameters sont généralement ajoutés à la fin de l'URL . ex: http://localhost:3000/reviews?query=first changer la fonction get dans reviews

export default function Home() {
  return (
     <>
     <h1>Home</h1></>
  );
}
