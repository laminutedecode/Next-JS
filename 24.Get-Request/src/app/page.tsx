// Une requête GET est une méthode de requête HTTP utilisée pour demander des données à partir d'un serveur web. Lorsqu'un client, tel qu'un navigateur web, envoie une requête GET à un serveur, il demande au serveur de lui renvoyer une ressource spécifique, comme une page web ou un fichier. Les paramètres de la requête GET sont envoyés dans l'URL sous forme de chaîne de requête, après le point d'interrogation (?). Cette méthode est souvent utilisée pour récupérer des données qui ne modifient pas l'état du serveur ou des données déjà existantes. Télégarger lextension thunder client.

// L'extension Thunder Client est un outil de test d'API HTTP. Il permet de tester et de déboguer des appels d'API directement depuis l'interface de VS Code.

export default function Home() {
  return (
     <>
     <h1>Home</h1></>
  );
}
